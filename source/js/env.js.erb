var ENV = window.ENV = {};

ENV.BUILD_ENV = '<%= ENV['BUILD_ENV'] %>';

ENV.LOG_VERSION = <%= ENV['BUILD_ENV'] == 'development' %>;

// TODO Set to `false`.
ENV.EXTEND_PROTOTYPES = {
  Array: true
};
